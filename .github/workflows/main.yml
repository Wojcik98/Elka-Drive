name: CI

on: [push]

jobs:
  integration:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install Qt
      uses: jurplel/install-qt-action@v2

    - name: Build app
      run: |
        qmake
        make
    - name: Run tests
      run: |
        ./ProcessResponse/test
